// import ProjectList from "../components/ProjectList";
import Parallax from "../components/Parallax";

function Home({ project, competence }) {
  return <Parallax project={project} competence={competence} />;
}

export default Home;
